<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_xs"
    app:cardBackgroundColor="?attr/colorSurface"
    app:cardCornerRadius="@dimen/corner_sm"
    app:cardElevation="@dimen/elevation_xs"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/spacing_sm">

        <!-- Log Level Indicator -->
        <View
            android:id="@+id/level_indicator"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="@color/log_info"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:background="@color/log_error" />

        <!-- Timestamp -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/text_timestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_sm"
            android:fontFamily="monospace"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintStart_toEndOf="@id/level_indicator"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="2026-01-03 14:32:45" />

        <!-- Log Level Badge -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/text_level"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_sm"
            android:background="@drawable/bg_badge"
            android:fontFamily="monospace"
            android:paddingHorizontal="@dimen/spacing_xs"
            android:paddingVertical="@dimen/spacing_xxs"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/text_timestamp"
            app:layout_constraintTop_toTopOf="parent"
            tools:backgroundTint="@color/log_error"
            tools:text="ERROR" />

        <!-- Log Message -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/text_message"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_sm"
            android:layout_marginTop="@dimen/spacing_xs"
            android:fontFamily="monospace"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurface"
            android:textIsSelectable="true"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/level_indicator"
            app:layout_constraintTop_toBottomOf="@id/text_timestamp"
            tools:text="Camera preview intercepted for com.example.app - injecting custom image from /sdcard/CameraInterceptor/image.jpg" />

        <!-- Tag (optional) -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/text_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_sm"
            android:layout_marginTop="@dimen/spacing_xxs"
            android:fontFamily="monospace"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textColor="?attr/colorPrimary"
            android:visibility="gone"
            app:layout_constraintStart_toEndOf="@id/level_indicator"
            app:layout_constraintTop_toBottomOf="@id/text_message"
            tools:text="CameraHook"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
